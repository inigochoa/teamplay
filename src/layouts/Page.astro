---
import Layout from './Layout.astro'

const { frontmatter } = Astro.props
---
<Layout title={frontmatter.title}>
  <article class="prose prose-lg">
    <h1>{frontmatter.title}</span></h1>
    <slot />
  </article>
</Layout>
